export enum SortMode {
    Ascending = "asc",
    Descending = "desc",
    NotSelected = "not"
}

export enum SortableField {
    Name = "name",
    Type = "type",
    Date = "date",
    ID = "id"
}
